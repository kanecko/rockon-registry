{
  "Copyparty": {
    "description": "Copyparty is a lightweight file sharing hub with accelerated resumable uploads, deduplication, WebDAV, zeroconf, a media indexer, image/audio/video thumbnails and audio transcoding. It is served over a single HTTP port and needs no client software beyond a web browser.<p>The container runs as the owner and group of the config Share, so that account must have read and write access to every Share mapped into the container.</p><p>Note: You must add a config file (copyparty.conf) to your copyparty Config [e.g. copyparty-config] Share. <a href='https://github.com/9001/copyparty/blob/hovudstraum/docs/examples/docker/basic-docker-compose/copyparty.conf' target='_blank'>Example</a>.</p><p>Based on the <a href='https://hub.docker.com/r/copyparty/ac' target='_blank'>official OCI image</a>, available for amd64 and arm64 architecture.</p>",
    "version": "1.0",
    "website": "https://github.com/9001/copyparty",
    "icon": "https://raw.githubusercontent.com/9001/copyparty/hovudstraum/docs/logo.svg",
    "opts": [
        ["--stop-timeout", "15"]
    ],
    "containers": {
      "copyparty": {
        "image": "copyparty/ac",
        "tag": "latest",
        "launch_order": 1,
        "uid": -1,
        "gid": -1,
        "ports": {
          "3923": {
            "description": "Port serving the copyparty Web-UI, its HTTP API, and WebDAV.",
            "label": "WebUI port [e.g. 3923]",
            "host_default": 3923,
            "protocol": "tcp",
            "ui": true
          }
        },
        "volumes": {
          "/cfg": {
            "description": "Copyparty configuration files.",
            "label": "Config Storage [e.g. copyparty-config]"
          },
          "/w": {
            "description": "The root of the shared file tree.",
            "label": "Data Storage [e.g. copyparty-data]"
          }
        },
        "environment": {
          "PYTHONUNBUFFERED": {
            "description": "Set to 1 to have copyparty write its log messages immediately rather than buffering them, at the cost of a small amount of speed. Set to 0 to keep buffering enabled.",
            "label": "Unbuffered log output [e.g. 0]"
          }
        }
      }
    }
  }
}
